<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/randevuFragment">

    <fragment
        android:id="@+id/randevuFragment"
        android:name="hasan.gurgur.enerjisatask.ui.randevu.RandevuFragment"
        android:label="fragment_randevu"
        tools:layout="@layout/fragment_randevu" />
    <fragment
        android:id="@+id/formFragment"
        android:name="hasan.gurgur.enerjisatask.ui.form.FormFragment"
        android:label="fragment_form"
        tools:layout="@layout/fragment_form" />

    <action
        android:id="@+id/action_global_formFragment"
        app:destination="@id/formFragment" />
</navigation>